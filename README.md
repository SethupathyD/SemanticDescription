# SemanticDescription
Setting SemanticProperties.DescriptionProperty On Windows, a custom grid-like layout reproduces a duplicate screen reader announcement when semantics are applied to the cell container rather than the inner Label
